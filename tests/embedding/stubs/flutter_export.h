// Stub flutter_export.h for unit tests.
// The real header dllexports symbols from flutter_windows.dll; in tests we
// compile the stub implementations directly, so no export decoration is needed.
#pragma once

#define FLUTTER_EXPORT
